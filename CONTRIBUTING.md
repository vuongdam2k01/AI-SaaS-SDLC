# Contributing

1. Open an issue describing the affected flow, artifact or deterministic rule.
2. Keep changes focused and add tests for behavior changes.
3. Run `npm run check`, `claude plugin validate . --strict` and `npm run validate:manifests`.
4. Use conventional commit messages. Do not include secrets or private product evidence.
